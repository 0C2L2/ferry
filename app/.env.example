# Copy to .env. Points the desktop app at the Ferry server (../assist-server),
# which mints the scoped keys for Cloud Backup. Leave unset to hide the cloud
# screens — the rest of Ferry works fully offline without it.
VITE_ASSIST_SERVER_URL=http://localhost:8787
