# Copy to .env. Points the desktop app at the optional Ferry Assist sidecar
# (see ../assist-server). Leave unset to disable the AI-assist / share-link
# features entirely — the rest of Ferry works fully offline without it.
VITE_ASSIST_SERVER_URL=http://localhost:8787
