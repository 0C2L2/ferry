<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <!-- Ferry needs administrator rights for every real run: diskpart
       (clean/create partition/format), writing raw partition contents, and
       mounting/copying OS-image boot files all require elevation. Without
       this, prepare_usb's diskpart calls fail with an access-denied style
       error and there is no way for a non-technical user to know to
       right-click "Run as administrator" themselves — so Windows must
       prompt for it automatically via this manifest instead. -->
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
    <security>
      <requestedPrivileges>
        <requestedExecutionLevel level="requireAdministrator" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
</assembly>
